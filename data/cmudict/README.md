This folder is optional.

MVP recommended approach:
- Use the Python package `cmudict` (installed via pip).
- No manual download is required.

If you choose to store a local copy of the CMU dictionary text file later,
place it here and add a manifest noting the version/source.

