[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["venv", ".venv", "notebooks", "data", "models", "reports"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
pythonpath = ["."]
